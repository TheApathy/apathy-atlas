{"id": "longctx-recall", "prompt": "You are given a document. Read it carefully.\n\n[DOC] The Aurora project began in March. The lead engineer was Dr. Mei Chen. The budget was 4.2 million dollars. The primary risk identified was thermal throttling on the GB10 board under sustained load. The mitigation chosen was a per-layer KV precision schedule. The launch date was set for the following November. [/DOC]\n\nBased only on the document above, answer: who was the lead engineer, what was the budget, and what mitigation was chosen? Answer concisely."}
{"id": "longctx-reasoning", "prompt": "Consider a system with 64 layers, of which every 4th layer (the 4th, 8th, 12th, ... , 64th) uses full attention and the rest use linear attention. How many full-attention layers are there, and what are the indices of the first three and the last one? Show your reasoning step by step."}
{"id": "longctx-continuation", "prompt": "Continue the following technical passage coherently for a few sentences, staying strictly on topic.\n\nThe key insight behind trellis-coded quantization for KV caches is that adjacent cache entries are correlated, so a Viterbi search over a coding trellis can allocate bits more efficiently than scalar per-element quantization. At 3.25 bits per value this is nearly lossless, while"}
{"id": "longctx-json", "prompt": "Extract the following into strict JSON with keys layer_count, full_attention_count, kv_dtype. Text: 'The model has 64 layers total, 16 of which are full attention, and the KV cache is stored in fp8.' Output only the JSON object."}
{"id": "longctx-code", "prompt": "Write a Python function `most_sensitive_layers(gaps: dict[int, float], n: int) -> list[int]` that returns the n layer indices with the largest gap-recovery values, sorted ascending. Include a docstring."}
