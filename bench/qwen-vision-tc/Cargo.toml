[package]
name = "qwen-vision-tc-bench"
version = "0.1.0"
edition = "2024"
publish = false
license = "AGPL-3.0-only"

# Standalone CPU admission tests: do not build Atlas or discover CUDA.
[workspace]

[lib]
path = "src/lib.rs"
